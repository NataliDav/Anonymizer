# Отчёт о приёмке «Локальный анонимизатор таблиц» v1.0

**Дата:** 25.08.2026 · **Метод:** приёмочные испытания строго по `ТЗ_Локальный_анонимизатор_таблиц.md`
**Среда:** Linux, Python 3.12.7, openpyxl 3.1.5, DISPLAY :0 (GUI запущен, PID активен)

---

## 1. Матрица приёмки: AC-ID → шаги → ожидаемый результат → статус

Обозначения: ✅ пройдено (авто) · 🟡 частично · 👁 ручная · ❌ провалено

### Функционал «Анонимизация» (ТЗ §5.1)

| AC-ID | Шаги | Ожидаемый результат (ТЗ) | Статус |
|---|---|---|---|
| AC-F1-01 | Загрузить .xlsx и .csv (UTF‑8, cp1251, `,`/`;`) | Файлы читаются, кодировка/разделитель определяются автоматически | ✅ юнит-тесты + e2e |
| AC-F1-02 | Открыть файл в GUI | Превью 10 строк + список столбцов с чекбоксами | 👁 ручная |
| AC-F1-03 | Проверить автопрефиксы и их редактирование | `FIO_/ADR_/TEL_`, поле редактируемое | ✅ логика (юнит); 👁 UI |
| AC-F1-04 | Ввести пароль <8 симв.; разные пароли | Отказ с сообщением; подтверждение обязательно | ✅ CLI (≥8); 👁 подтверждение |
| AC-F1-05 | Анонимизировать эталон | Замена строго по алгоритму §6 | ✅ e2e |
| AC-F1-06 | Сохранение | `<имя>_anon.xlsx` + `<имя>_KEY.kvault` рядом с исходником | ✅ |
| AC-F1-07 | Прочитать итоговый отчёт | Статистика по столбцам + пути файлов | ✅ |
| AC-F1-08 | Пустые/пробельные ячейки | Остаются пустыми, пропуски в отчёте | ✅ |

### Функционал «Восстановление» (ТЗ §5.2)

| AC-ID | Шаги | Ожидание | Статус |
|---|---|---|---|
| AC-F2-01 | Загрузить `_anon` + `.kvault` | Оба файла принимаются | ✅ |
| AC-F2-02 | Неверный пароль | Понятная ошибка, без частичного восстановления | ✅ rc=2, файл не создан |
| AC-F2-03 | Восстановление | Индекс → исходное значение | ✅ 60/60 ячеек |
| AC-F2-04 | Подменить значение в ключе | Предупреждение **с указанием ячейки** | 🟡 указываются столбец+индекс, без строки/ячейки |
| AC-F2-05 | Внести неизвестный индекс | Маркер `[?? НЕИЗВЕСТНО]` + перечень | ✅ rc=1 |
| AC-F2-06 | Сохранение результата | `<имя>_restored.xlsx` | ✅ |

### Общие требования (ТЗ §5.3)

| AC-ID | Шаги | Ожидание | Статус |
|---|---|---|---|
| AC-F3-01 | Выполнить операции, открыть `anonymizer.log` | Все операции залогированы, ПДн в логе нет | ❌ файл не создаётся вовсе (см. D2) |
| AC-F3-02 | Повторный encode того же файла | Идентичные индексы | ✅ наблюдаемо; 🟡 режим переиспользования соли ключа отсутствует |
| AC-F3-03 | Повторы значений | Одно значение → один индекс | ✅ |
| AC-F3-04 | Запуск `python anonymizer.py encode…` | CLI работает | 🟡 работает `python -m anonymizer.main`, `anonymizer.py` отсутствует |

### Алгоритм (ТЗ §6) и хранилище

| AC-ID | Шаги | Ожидание | Статус |
|---|---|---|---|
| AC-A-01 | Формат индексов | `PREFIX_%04d` по первому появлению | ✅ `FIO_0001` |
| AC-A-02 | Структура `.kvault` | JSON: format/created/source_file/columns/entries | ✅ |
| AC-A-03 | Шифрование | PBKDF2‑HMAC‑SHA256 600k + Fernet, соль 16 Б | ✅ код+тесты |
| AC-A-04 | Нормализация | NBSP→пробел, lower, схлопывание пробелов | ✅ |

### Нефункциональные (ТЗ §9)

| AC-ID | Шаги | Ожидание | Статус |
|---|---|---|---|
| AC-N-01 | Обработать 100 000×50 | ≤ 30 с | 🟡 CSV: 5,1 c ✅; XLSX: **84,8 с** ❌, RAM 2,4 ГБ (см. D1) |
| AC-N-02 | Размер файла-ключа | ~≤ 200 МБ | ✅ 12,9 МБ на 100 тыс. значений |
| AC-N-03 | Кроссплатформенность | Win/Linux(/macOS) | 👁 Linux ✅; Win/macOS вне среды |
| AC-N-04 | Запрет сети | Нет сетевых импортов + автотест | ✅ grep + `test_no_network_imports` |
| AC-N-05 | Пароль не на диске | Только производный токен | ✅ тест; 🟡 затирание переменной не реализовано; `--password` виден в `ps` |
| AC-N-06 | Юникод/NBSP | Корректная обработка | ✅ телефоны эталона |
| AC-N-07 | CSV UTF‑8 BOM, cp1251 | Да | ✅ |

### Обработка ошибок (ТЗ §10)

| AC-ID | Шаги | Ожидание | Статус |
|---|---|---|---|
| AC-E-01 | Скормить битый .xlsx | Сообщение без трейсбека | ✅ rc=2 |
| AC-E-02 | Пустой файл / несуществующий столбец | Предупреждение, отказ | ✅ rc=2 |
| AC-E-03 | Ни одного отмеченного столбца (GUI) | Диалог-напоминание | 👁 ручная |
| AC-E-04 | Пароль <8 / несовпадение | Диалог валидации | ✅ CLI (<8); 👁 GUI |
| AC-E-05 | Неверный пароль .kvault | Отказ целиком | ✅ |
| AC-E-06 | Ключ от другой таблицы | Предупреждение **+ предложение продолжить/отменить** | 🟡 предупреждение есть после факта, диалога нет (D3) |
| AC-E-07 | Запись в защищённую папку | Предложение выбрать другое место | 🟡 только общая ошибка (D4) |

### Интерфейс (ТЗ §8)

| AC-ID | Элемент | Статус |
|---|---|---|
| AC-G-01 | Две вкладки, состав элементов обеих вкладок | 👁 структурно реализовано |
| AC-G-02 | Индикатор надёжности пароля (§8.1 п.3) | ❌ отсутствует (D7) |
| AC-G-03 | Кнопка «Сохранить как…» на вкладке анонимизации (§8.1 п.4) | ❌ отсутствует (D6) |
| AC-G-04 | Журнал предупреждений на вкладке восстановления | 👁 реализован |

### Сценарии приёмки (ТЗ §12)

| AC-ID | Сценарий | Статус |
|---|---|---|
| AC-S12-1 | Полный цикл на эталоне: `FIO_0001/ADR_0001/TEL_0001`, числа не тронуты | ✅ |
| AC-S12-2 | Повторяемость индексов | ✅ |
| AC-S12-3 | `_restored` == оригинал во всех ячейках | ✅ |
| AC-S12-4 | Подмена буквы → предупреждение, остальное восстановлено | ✅ |
| AC-S12-5 | Изоляция: нет сетевых импортов | ✅ статически; отключение Wi-Fi — 👁 |
| AC-S12-6 | Произвольный csv без правок кода | ✅ (`edge.csv`: другие столбцы, пустые ячейки) |

---

## 2. Выполненные автоматические проверки

1. Юнит- suite: `python3 -m unittest discover -s anonymizer/tests` — **28/28 OK** (движок, vault, io, запрет сети).
2. E2E-цикл на `Data_unput.xlsx` (20 строк): encode → decode → **полное поэлементное совпадение**, числовые столбцы неизменны, пути по умолчанию созданы рядом с исходником.
3. Детерминированность: повторный encode дал идентичную анонимную таблицу.
4. Отказоустойчивость: неверный пароль (rc=2, артефактов нет); подмена индекса (rc=1, маркер `[?? НЕИЗВЕСТНО]`, предупреждение, остальные строки целы); подмена значения в ключе → предупреждение о расхождении отпечатка.
5. Граничные данные: csv с пустыми/пробельными ячейками и незнакомыми столбцами — корректно (roundtrip равен оригиналу).
6. Повреждённый xlsx и пустой csv — чистые ошибки rc=2 без трейсбеков.
7. Производительность (100 000×50): CSV encode 5,14 с / decode 5,06 с / RAM ~0,5 ГБ; XLSX encode **84,78 с** / RAM 2,4 ГБ.
8. Статический анализ: сетевые импорты отсутствуют; размер `.kvault` 12,9 МБ на 100 тыс. записей; `anonymizer.log` не создаётся; вызовы logging в cli.py отсутствуют.

## 3. Ручные проверки (ненадёжно автоматизируемые)

1. Визуальная компоновка вкладок, читаемость превью, работа скроллингов (AC-F1-02, AC-G-01).
2. Диалоговые окна messagebbox при успехе/ошибках в GUI (AC-E-03/04).
3. Поток «Обзор → чекбоксы → редактирование префикса → запуск» полностью мышью.
4. Изоляция при физически отключённом Wi-Fi/Ethernet (вторая часть AC-S12-5).
5. Запуск на Windows и macOS (AC-N-03).
6. Поведение при отказе диска/прав доступа в момент сохранения через GUI (AC-E-07).

---

## 4. Дефекты

### Блокирующие
Нет.

### Высокие
- **D1 [AC-N-01, N1]** Нарушен лимит производительности для XLSX: полный цикл encode на 100 000×50 — **84,8 с при норме ≤ 30 с** (в 2,8 раза), пик памяти 2,4 ГБ. Для CSV требование выполнено (5,1 с). Обходной путь: конвертация в CSV либо ожидание.

### Средние
- **D2 [AC-F3-01, F3.1]** Логирование операций фактически отсутствует: в cli.py нет вызовов logging; `main.py` создаёт `App()` напрямую, минуя `launch()` с настройкой `basicConfig` — файл `anonymizer.log` не создаётся ни одним режимом.
- **D3 [AC-E-06, §10]** При несовпадении столбцов таблицы и файла-ключа нет предупреждающего диалога «продолжить/отменить» до операции; восстановление выполняется, предупреждения выводятся постфактум.
- **D4 [AC-E-07, §10]** При ошибке записи (защищённая папка и т. п.) не предлагается выбрать другое место сохранения — выводится только общее сообщение об ошибке.
- **D5 [AC-F2-04, F2.4]** Предупреждение о расхождении контрольного отпечатка называет столбец и индекс (««ФИО» FIO_0001»), но не конкретную ячейку/строку, как требует ТЗ.

### Низкие
- **D6 [AC-G-03, §8.1]** На вкладке «Анонимизация» нет кнопки «Сохранить как…»: пути результатов жёстко фиксируются рядом с исходником, опции «по выбору пользователя» (F1.6) нет.
- **D7 [AC-G-02, §8.1]** Отсутствует индикатор надёжности пароля.
- **D8 [AC-F3-04, F3.4]** Заявленная форма запуска `python anonymizer.py …` не поддерживается (только `python3 -m anonymizer.main`).
- **D9 [AC-F3-02, F3.2]** Режим продолжения сессии с прежней солью/ключом не реализован; идентичность индексов обеспечивается порядком появления (наблюдаемый результат ТЗ выполняется).
- **D10 [AC-N-05, N5]** Переменная пароля не затирается в памяти; передача `--password` в CLI видна в списке процессов (`ps`).

---

## 5. Заключение

**ПРИНЯТО С ЗАМЕЧАНИЯМИ.**

Ядро соответствует ТЗ: алгоритм §6 воспроизведён точно, шифрование файла-ключа корректно, обратное восстановление даёт байт-в-байт совпадение с оригиналом, сетевая изоляция подтверждена статически и автотестом, сценарии §12.1–12.6 пройдены.

Непройденные требования (основания для замечаний):
- **N1** — количественный лимит 30 с нарушен для формата XLSX на максимальном объёме (D1);
- **F3.1** — журналирование операций не функционирует ни в одном режиме (D2);
- **F2.4** — частично: предупреждения без координат ячейки (D5);
- **§10** (две строки таблицы) — частично: нет диалога продолжить/отменить и предложения альтернативного места сохранения (D3, D4);
- **§8.1** — отсутствуют два элемента интерфейса: индикатор надёжности пароля и «Сохранить как…» (D6, D7);
- **F3.4, F3.2, N5** — незначительные отклонения формы реализации (D8–D10).

Рекомендация: устранить D1–D5 до продуктивного использования на больших XLSX-файлах; D6–D10 допустимо перенести в ближайший релиз сопровождения.
